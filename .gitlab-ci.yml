image: docker

services:
  - docker:dind

before_script:
  - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY

build:
  script:
          #- docker pull registry.gitlab.com/sscottie/skillbox_devops_hometask
    - docker build -t registry.gitlab.com/sscottie/skillbox_devops_hometask .
    - docker push registry.gitlab.com/sscottie/skillbox_devops_hometask
